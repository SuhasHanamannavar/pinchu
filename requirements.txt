PyQt5>=5.15.0
aiohttp>=3.9.0
python-dotenv>=1.0.0
psutil>=5.9.0
cognee>=1.0.0
SpeechRecognition>=3.10.0
pyttsx3>=2.90

# macOS-specific: install pyaudio separately
# pip install pyaudio  (may need brew install portaudio on macOS)
